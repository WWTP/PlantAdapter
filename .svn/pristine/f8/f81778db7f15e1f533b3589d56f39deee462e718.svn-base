package tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

// Testbanch che invia comandi e risceve risposte a questi e schedules
// ma NON effettua controlli (assert) sui risultati (serve solo avedere se tutto funziona
// o se qualcosa si blocca). Nota: nel file PlantAdapterTest.java c'è un esempio della stampa
// a console nel caso di test eseguito con successo.

@RunWith(Suite.class)
@SuiteClasses
({	
	// PlantAdapter
	tests.plantadapter.PlantAdapterTest.class
})
public class PlantAdapterTests {

}