package plantmodel;

/**
 * Indica la direzione con cui si sta operando in IO
 */
// TODO da utilizzare nel DT80 per gestire i ChannelType (ora accesso diretto all'enumeratore).
public enum IODirection {
	INPUT,
	OUTPUT,
	BOTH
}
